import styled from '@emotion/styled'
import { FiDatabase } from 'react-icons/fi'

export default styled(FiDatabase)`
  font-size: 48px;
  margin-right: 16px;
  color: ${({ theme }) => theme.color.white[100]};
`
