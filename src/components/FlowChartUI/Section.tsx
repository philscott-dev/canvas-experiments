import styled from '@emotion/styled'
import { ExpandType } from '../FlowChartDetailPanel'

export default styled.section<{ expand: ExpandType }>`
  box-sizing: border-box;
  display: flex;
  flex: 1;
`
